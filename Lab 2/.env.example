# Server configuration
SERVER_PORT=8000
# Host port mapping when using docker-compose
HOST_PORT=8000
DOC_ROOT=/srv/www
# artificial delay per request in seconds (float)
HANDLER_DELAY=1.0
# counter mode: naive or locked
COUNTER_MODE=locked
# rate limit requests per second per IP (float)
RATE_LIMIT_RPS=5.0
# burst capacity for token bucket
RATE_LIMIT_BURST=10
# server type: single or threaded
SERVER_TYPE=threaded
# When behind proxy or for testing, trust X-Forwarded-For for client IP extraction
TRUST_XFF=false